# Source corpus: external texts, several still in copyright.
# Kept out of the public repo; only metrics derived from them are committed.
corpus/

# DuckDB warehouse file (regenerated by `dbt run`)
*.duckdb
*.duckdb.wal
